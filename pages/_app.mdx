import { Analytics } from '@vercel/analytics/react';
import NextTopLoader from 'nextjs-toploader';
import '../styles.css';

export default function App({ Component, pageProps }) {
  return (
    <>
      <Analytics />
      <NextTopLoader color='#006CE6' showSpinner={true} />
      <Component {...pageProps} />
    </>
  );
}
